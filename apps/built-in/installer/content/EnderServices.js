const sendPermissions = (ID, permissions, callback) => {
    //
    setTimeout(function() {
        callback();
    }, 500);
}
const sendModules = (ID, modules, callback) => {
    //
    setTimeout(function() {
        callback();
    }, 500);
}